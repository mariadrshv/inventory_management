{% extends 'item/breadcrumbs.html.twig' %}

{% block title %}Edit Item{% endblock %}

{% block body %}
    <h1>Edit Item</h1>

    {{ include('item/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('item_index', {'room_id': room.id, 'prop_id': property.id} )}}">back to list</a>

    {{ include('item/_delete_form.html.twig') }}
{% endblock %}
