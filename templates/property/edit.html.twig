{% extends 'property/breadcrumbs.html.twig' %}

{% block title %}Edit Property{% endblock %}

{% block body %}
    <h1>Edit Property</h1>

    {{ include('property/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('property_index') }}">back to list</a>

    {{ include('property/_delete_form.html.twig') }}

    {{ form_start(form) }}

        {{ form_row(form.photo) }}


    {{ form_end(form) }}

    <a href="{{ path('app_logout') }}">Logout</a>
{% endblock %}
