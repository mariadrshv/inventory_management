{% extends 'base.html.twig' %}

{% block title %}New Room{% endblock %}

{% block body %}
    <h1>Create new Room</h1>

    {{ include('room/_form.html.twig') }}

    <a href="{{ path('room_index', {'room_id': room.id, 'prop_id': property.id}) }}">back to list</a>
{% endblock %}
